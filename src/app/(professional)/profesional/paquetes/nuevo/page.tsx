import { PackageWizard } from '@/components/professional/packages/package-wizard';

/**
 * Page for creating a new session package via the multi-step wizard.
 *
 * Validates: Requirements 1.1
 */
export default function NuevoPaquetePage() {
  return <PackageWizard />;
}
